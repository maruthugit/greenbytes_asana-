; Project-local PHP config for this Laravel app only.
; Use via: php -c .\php.local.ini artisan <command>

; Point directly at Scoop's extension folder (does NOT change global PHP settings).
extension_dir = "C:\\Users\\User\\scoop\\apps\\php82\\current\\ext"

; Common Laravel requirements
extension=openssl
extension=mbstring
extension=curl
extension=fileinfo
extension=zip
extension=intl

; Database drivers
extension=pdo_mysql
extension=mysqli

; Optional (handy for tests / tooling)
extension=pdo_sqlite
